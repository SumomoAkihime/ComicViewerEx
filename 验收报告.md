# ComicViewerEx 1.0 验收报告

验收日期：2026-09-16 至 2026-09-17。

## 环境与结论

- 实测系统：Windows 11 家庭版 x64，10.0.26200.8655。
- 硬件：AMD Ryzen AI 9 HX 370、24 GiB 内存、本地 SSD。
- 构建：MSVC 19.44、Windows SDK 10.0.26100.0，Release x64，静态 C/C++ 运行库。
- 核心自动测试 4/4 通过；真实 Win32 界面自动验收通过，截图已检查。
- 程序 EXE 与完整 `7z.dll` 合计约 3.46 MiB，运行无需联网或预装开发运行时。

## 性能

样本为自生成的 200 页漫画 ZIP，每页 2000×3000 PNG；包含高 DPI 原生窗口。内存验收指标为进程私有提交内存，工作集另外列出。

| 指标 | 目标 | 实测 |
|---|---|---|
| 首次启动至窗口可操作 | ≤1000 ms | 467 ms |
| 重复启动，3 次 | ≤1000 ms | 320–344 ms |
| 未打开漫画时私有内存 | ≤60 MiB | 37.15 MiB |
| 普通阅读私有内存 | ≤256 MiB | 162.09 MiB |
| 缓存翻页，10 次 | ≤100 ms | 48.52–64.25 ms |
| 空闲 / 阅读工作集 | 记录值 | 68.49 / 161.03 MiB |

启动计时包含创建进程、发现主窗口与等待输入空闲；首次启动使用新的便携目录，但未清除操作系统文件缓存，因此不等同于重启系统后的严格冷启动。翻页计时包含页码确认、阅读区重绘及 DWM 合成等待；不包含显示器扫描输出。以上数字只代表本机与该样本，不外推到机械盘、固实包首次解码和超大图片。

## 已通过的场景

- ZIP/CBZ、ZipCrypto、AES ZIP；RAR4/RAR5 普通、固实、内容加密、文件名加密；普通和文件名加密 7z。
- 错误密码提示；取消读取；分卷、损坏包、没有图片的压缩包明确报错。
- JPEG、PNG、BMP、GIF、TIFF、WebP 静态图的尺寸和像素检查，WebP 透明预乘，损坏图片和取消检查。
- 中文目录、自然排序、图片目录递归；单页、双页、横图单独显示、宽图前后半页和阅读位置恢复。
- 密码未勾选保存时不落库；验证成功保存、重开自动解锁；密码增删改的数据层检查。
- 标签创建、重命名、批量关联/移除、全部/任一匹配、无筛选；同卷改名后保留关联；目录阅读状态持久化。
- 实际界面创建标签、给文件打标、应用筛选和清除筛选；密码弹窗输入、保存和再次自动解锁。
- 只读数据库操作明确失败，不显示保存成功。
- 60 张约 9 MiB 图片的固实包触发真实磁盘缓存淘汰；缓存保持在 512 MiB 内，关闭后清空，重复解压可取消。
- 连续快速切页只采用最新请求；40 次普通/加密书籍交替打开后的句柄数量未持续增加。
- 12 轮全屏进入/退出后，USER 资源计数为 97 → 97。

## 边界与未覆盖项

- Windows 10 未进行实机验收；代码目标为 Windows 10/11 x64。
- 未实测机械盘、网络共享、跨卷移动、严格系统冷启动、所有压缩算法变体及损坏形式。
- GIF/TIFF 仅首帧；WebP 动画首帧实现存在，但本轮自动图像测试覆盖静态 WebP，不将动画素材标记为已验收。
- 600 万像素以上图片会按内存预算降采样；1 亿像素以上或单图数据过大时拒绝读取。保留原始尺寸用于比例布局，不承诺大图放大后的原始细节。
- 密码按用户选择明文存储，可随便携目录转移；不会写入日志或本报告。

## 验收证据

- 核心测试：`build/Testing/Temporary/LastTest.log`。
- 界面与性能原始记录：`test-output/gui-20260916-235859/results.json`。
- 同目录 `01-empty.png` 至 `06-tags.png`：空白、阅读、双页、全屏、分页与标签界面。
- RAR 样本出处与摘要：`test-output/fixtures/sources.json`。测试样本不进入便携包。
- 构建、样本准备、界面验收脚本随完整源码包提供，可复验。
