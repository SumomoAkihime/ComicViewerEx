# ComicViewerEx 1.1.0 验收报告

验收日期：2026-09-17。本报告记录本次 Release 构建结果，不沿用旧版本性能数据。

## 环境与结论

- Windows 11 家庭版 x64，10.0.26200.8655；AMD Ryzen AI 9 HX 370、24 GiB 内存、本地 SSD。
- MSVC 19.44、Windows SDK 10.0.26100.0，Release x64，静态 C/C++ 运行库。
- Release 编译通过；清理新增代码局部变量重名警告。核心测试 4/4 通过，用时 5.34 秒；之后仅调整底部气泡布局，重新构建并验收界面。
- 画布／标签界面与侧栏／列表自动验收通过。侧栏实测 DPI 为 192（200%），截图已检查。
- EXE 与完整 `7z.dll` 合计 3,675,136 字节，运行无需联网或预装开发运行时。

## 性能

样本为自生成的 200 页 ZIP，每页 2000×3000 PNG。内存为进程私有提交内存。

| 指标 | 目标 | 本机实测 |
|---|---|---|
| 新便携目录首次窗口启动 | ≤1000 ms | 538.16 ms |
| 重复启动，3 次 | ≤1000 ms | 403.36–424.30 ms |
| 空闲私有内存 | ≤60 MiB | 37.45 MiB |
| 普通阅读私有内存 | ≤256 MiB | 160.40 MiB |
| 缓存翻页，10 次 | ≤100 ms | 48.63–50.48 ms |
| 空闲 / 阅读工作集 | 记录值 | 65.02 / 158.73 MiB |

启动计时包含创建进程、发现主窗口与等待输入空闲，不代表后台恢复漫画完成。未清除系统文件缓存，不等同于严格冷启动。空闲内存等待 3 秒后采样，避免使用组件初始化前的低值。

翻页计时包含页码确认、阅读区重绘及 DWM 合成等待，不包含显示器扫描输出。上述数据不外推到机械盘、固实包首次解码和超大图片。

## 已通过的回归

- 存储：多标签、重复添加不覆盖、批量添加／移除、含逗号标签名称、全部／任一匹配、同卷改名、只读数据库报错；全局阅读偏好迁移、持久化和异常值回退。
- 图片／压缩／调度：既有四项核心测试中的静态图片、ZIP/RAR/7z 固定样本、密码、自然排序、固实缓存限制和快速请求取消回归。
- 画布左右点击、轻微移动、拖动不翻页、双击翻页、固定方向和中键全屏通过。
- 普通阅读、连续回翻、双页与全屏窗口命令通过。
- 重启恢复上次书籍与双页模式通过。
- 标签气泡鼠标点击、键盘激活和筛选同步通过。
- 宽图分页、同文件多标签、独立打标、即时筛选和独立清除筛选通过。
- 文件名加密 7z 密码输入、保存及再次自动解锁通过。
- 左右停靠、调宽及 280 DIP 最小宽度通过。
- 中文目录下拉展开、Enter 确认、Esc 取消通过。
- 列表单击选择、上下键连续切书、焦点保持及全局双页通过。
- 重启恢复侧栏方向、宽度与全局双页通过。
- 40 个长标签保持单行高度、键盘滚至末项、滚轮后鼠标准确筛选通过。

- 底部气泡高度限制为单行（测试断言不超过 40 DIP），与文件信息并排；标签点击与键盘激活仍能筛选。
- 12 轮全屏进入／退出后，USER 资源计数 110 → 110。

界面脚本向本程序的原生窗口发送输入消息并检查状态、数据库及布局。测试脚本曾因同步发送列表鼠标按下而阻塞，另一次模拟光标位置不一致导致画布手势超时；已修正输入方式并重新运行通过，失败运行不作为通过证据。

## 边界与待验收项

- Windows 10、100%／150% DPI、跨屏 DPI、严格冷启动、机械盘、网络共享、跨卷移动未实测。
- 多选焦点的全部 Ctrl/Shift 组合、目录下拉外部点击及不可访问目录、加载途中连续切换各模式，仍需按交互检查表补充人工回归。
- 全部／任一匹配和批量标签关联通过存储层测试，不代表全部界面组合已实测。
- GIF/TIFF 仅首帧。600 万像素以上图片降采样；1 亿像素以上或单图数据过大时拒绝读取，不承诺超大图放大后的原始细节。
- 固实包首次解码、超大图片未在本次交互发布中重新测量性能，不套用上表数字。
- 密码按用户选择明文保存，不写入日志或报告。测试数据库、参考程序和本地验收输出不进入发布包。

## 验收证据

- 核心测试：`build/Testing/Temporary/LastTest.log`。
- 最终画布／标签与性能：`test-output/gui-20260917-162336/results.json`。
- 最终侧栏／列表：`test-output/sidebar-20260917-162318/results.json`。
- README 截图来自本次 GUI 验收的自生成图片和标签界面。
- RAR 样本出处与摘要：`test-output/fixtures/sources.json`。
- 自动验收脚本随完整源码包提供；原始测试输出和素材不随便携包发布。

## 便携包检查

便携包在全新隔离目录解压后，独立启动、读取、翻页、退出及重启恢复通过；EXE 和 7z.dll 与已验收构建产物逐字节一致。记录：`test-output/package-v1.1.0-20260917-162515/results.json`。
