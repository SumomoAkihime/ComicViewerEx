# ComicViewerEx v1.1.0

本次更新侧栏、阅读交互与多标签操作，适用于 Windows 10/11 x64。

## 更新内容

- 侧栏可左右拖动停靠、调整宽度并在重启后恢复；目录树改为路径下拉，释放文件列表空间。
- 点击画布左半边上一页、右半边下一页；左键拖动平移，中键全屏，左键双击按两次点击处理。
- 单击文件立即阅读，上下键连续切书；保持浏览目录、筛选与列表焦点，Ctrl/Shift 多选用于批量打标。
- 单页／双页／宽图分页、阅读方向和适应方式全局保存，切书不再恢复旧开关；各书保留进度与缩放倍数。
- 标签区分为“筛选”和“打标”两个独立页签：筛选勾选即时生效，批量打标只添加／移除指定标签，不覆盖其他关联。
- 底部标签气泡压缩为单行，与文件信息并排；标签增多只滚动查看，不增加高度挤占画布。点击或键盘激活气泡可切换筛选。

## 下载与升级

- `ComicViewerEx-1.1.0-win-x64.zip`：便携程序，解压运行 `ComicViewerEx.exe`。
- `ComicViewerEx-1.1.0-source.zip`：完整源码及固定版本依赖。
- `SHA256.json`：两个 ZIP 的 SHA-256 校验值。

升级前退出旧程序并备份原目录的 `data`，覆盖新版程序和随包文件，保留 `data` 即可继承标签、进度、设置和已保存密码。数据库版本不变；首次升级从上次阅读漫画的旧设置初始化全局阅读偏好。密码仍按用户选择明文保存。

GitHub 自动生成的源码包不含依赖，构建方式见 README。运行时保留完整 `7z.dll` 与许可文件，无需联网下载组件。

## 本次验证

Windows 11 x64 Release 编译、核心测试 4/4、画布／标签界面验收、200% DPI 下的侧栏／列表补充验收通过。

窗口首次启动 538.16 ms，重复启动 403.36–424.30 ms；空闲私有内存 37.45 MiB，普通阅读 160.40 MiB，缓存翻页 48.63–50.48 ms。

启动计时至窗口输入空闲，不包含后台恢复漫画；未清除系统文件缓存，不代表严格冷启动。Windows 10、跨屏 DPI 和跨卷移动未实测。详见随包《验收报告》和《交互调整验收》。
