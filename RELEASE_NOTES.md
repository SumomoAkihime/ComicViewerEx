# ComicViewerEx v1.0.0

首个中文 Windows 原生便携版，目标平台为 Windows 10/11 x64。

## 功能

- 直接读取 ZIP/CBZ、RAR/CBR、7z 和图片目录，支持加密压缩包及成功验证后的密码保存。
- 单页、双页、宽图分页、左右阅读方向、缩放拖动、全屏及阅读进度恢复。
- 标签创建、重命名、删除、批量打标与移除；支持全部/任一标签筛选及递归目录范围。
- 后台读取、96 MiB 图片缓存预算、512 MiB 固实包临时缓存上限。

## 下载与运行

- `ComicViewerEx-1.0.0-win-x64.zip`：便携程序，解压后运行 `ComicViewerEx.exe`，保留同目录 `7z.dll` 与许可文件。
- `ComicViewerEx-1.0.0-source.zip`：完整源码与固定依赖；构建步骤见 README。
- `SHA256.json`：上述两个 ZIP 的 SHA-256 校验值。

GitHub 自动生成的源码压缩包不附依赖。设置、标签、进度和用户选择保存的明文密码位于程序旁的 `data` 目录。

## 验证与限制

Windows 11 实测：启动 320–467 ms，空闲私有内存 37 MiB，普通阅读 162 MiB，缓存翻页 49–64 ms。核心测试 4/4 与界面验收通过，便携包解压后独立启动、读取与退出通过。

Windows 10、严格冷启动和跨卷移动尚未实测。GIF/TIFF 仅显示首帧；大于 600 万像素的图片降采样。不支持嵌套或分卷压缩包。详细样本、测量口径及边界见随包的《验收报告》。
