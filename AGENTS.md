# 项目约定

- 中文界面和说明；文本源码使用 UTF-8，读写文本显式指定编码。
- 最小必要改动，不修改“参考”目录。必要外部依赖及本次编译、运行验收已获得用户授权。
- C++20 / Win32 / Direct2D / WIC，Windows 10/11 x64 便携版。
- 已知事项：参考 ComicsViewer.htm 为 GB2312，ComicsViewer.ini 为 UTF-16；不得按 UTF-8 直接解析。
- 固实 RAR/7z 随机读取可能从固实块开头重新解码；不能承诺与 ZIP 相同跳页速度，必须可取消并限制缓存。
- 用户选择密码明文保存，不得将密码写入日志或测试报告；旧密码文件不导入。
- 子任务只修改明确分配的文件，不创建下级代理。
- PowerShell 中不可用反斜杠转义双引号；复杂 Python 代码写 UTF-8 脚本文件后执行，避免命令行正则字符串被 PowerShell 误解析。
- 标签区现已拆为“筛选”和“打标”两个独立勾选列表：只有筛选勾选即时扫描目录；打标勾选必须等添加/移除按钮确认，不能连带筛选掉尚未打标的文件。清除筛选不能清空打标选择。
- 7-Zip COM 回调存在多重继承，通过辅助基类 delete this 时必须有虚析构；否则释放内侧基类指针会造成堆损坏。接口签名以本项目锁定版本官方头为准。
- IInStream::Seek 的 newPosition 和 ISequentialStream 的 processedSize 允许为空，不能将空输出参数误判为解压失败；RAR5 签名为 52 61 72 21 1A 07 01 00，版本字节在索引 6。
- 7-Zip 的进度回调可与提取回调并发，取消状态必须原子化，进度节流状态需同步。
- 发布前发现上级 D:/project 自带其他项目的 Git 仓库；本项目必须使用自己的 .git，提交前核对 git rev-parse --show-toplevel，禁止操作上级仓库。公开提交排除“参考”、data、构建目录与本地验收输出。
- 完整源码包内含依赖自己的 README.md；校验项目文档必须匹配压缩包根目录完整路径，不能按文件名后缀取第一个匹配项。
- 列表打开漫画不能重设浏览目录或重新扫描，否则递归范围、滚动位置和多选会丢失；后台加载完成不能强制聚焦画布，否则上下键无法连续切书。
- 模式、方向与适应方式为全局偏好，旧书记录仅恢复进度和缩放；后台结果不得覆盖较新的界面偏好。侧栏控件的父窗口为 Sidebar，不能再用主窗口 GetDlgItem 查找它们。
- 目录弹树再次点击原入口时必须消费该次关闭点击，避免外部点击先隐藏 Popup、随后按钮命令又打开；Popup 向宿主转移激活时不能提前隐藏到点击预处理无法辨别。
- 一个文件通过 book_tags 关联多个标签；底部气泡使用真实标签 ID，不可拆分 bookTags 的逗号展示字符串（标签名称本身允许逗号）。气泡展示所选文件，批量选择时明确标注数量和焦点文件，不能误示为全部文件共有标签。
- Windows 上用 Python 修改现有 LF 文本时，除 encoding='utf-8' 外还应指定 newline='\n'，否则 write_text 默认换行转换会造成整文件 CRLF 差异。
- 标签气泡滚动后的命中测试必须限制在可见气泡区域，不能命中被标题遮住的标签；键盘焦点移动到屏外气泡时需同步滚动到可见位置。
- 标签气泡获得焦点时不能自动滚动，否则鼠标点击前布局改变可能命中其他标签；仅键盘导航同步滚动。气泡区域缩放后需重新布局并重绘。
- 底部标签区固定为一行高度，文件信息与气泡并排；标签增多时滚动查看，不得增加高度挤占阅读画布。
- 异步筛选刷新若未改变气泡文件上下文及标签条目，不应取消正在进行的气泡点击，否则快速筛选时会吞掉鼠标抬起；仅文件或条目实际变化时取消捕获。
- Win32 界面验收中，原生列表鼠标按下可能进入内部跟踪循环，跨进程点击应投递按下/抬起消息，不能同步阻塞在按下；测试画布捕获手势时须同步真实光标位置，避免系统补发的移动消息被误识别为拖动。
