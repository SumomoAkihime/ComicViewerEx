# 项目约定

- 中文界面和说明；文本源码使用 UTF-8，读写文本显式指定编码。
- 最小必要改动，不修改“参考”目录。必要外部依赖及本次编译、运行验收已获得用户授权。
- C++20 / Win32 / Direct2D / WIC，Windows 10/11 x64 便携版。
- 已知事项：参考 ComicsViewer.htm 为 GB2312，ComicsViewer.ini 为 UTF-16；不得按 UTF-8 直接解析。
- 固实 RAR/7z 随机读取可能从固实块开头重新解码；不能承诺与 ZIP 相同跳页速度，必须可取消并限制缓存。
- 用户选择密码明文保存，不得将密码写入日志或测试报告；旧密码文件不导入。
- 子任务只修改明确分配的文件，不创建下级代理。
- PowerShell 中不可用反斜杠转义双引号；复杂 Python 代码写 UTF-8 脚本文件后执行，避免命令行正则字符串被 PowerShell 误解析。
- 标签列表的选择同时用于打标，筛选必须点击“应用筛选”；不能在选中标签时立即清空未打标文件，导致无法首次打标。
- 7-Zip COM 回调存在多重继承，通过辅助基类 delete this 时必须有虚析构；否则释放内侧基类指针会造成堆损坏。接口签名以本项目锁定版本官方头为准。
- IInStream::Seek 的 newPosition 和 ISequentialStream 的 processedSize 允许为空，不能将空输出参数误判为解压失败；RAR5 签名为 52 61 72 21 1A 07 01 00，版本字节在索引 6。
- 7-Zip 的进度回调可与提取回调并发，取消状态必须原子化，进度节流状态需同步。
- 发布前发现上级 D:/project 自带其他项目的 Git 仓库；本项目必须使用自己的 .git，提交前核对 git rev-parse --show-toplevel，禁止操作上级仓库。公开提交排除“参考”、data、构建目录与本地验收输出。
- 完整源码包内含依赖自己的 README.md；校验项目文档必须匹配压缩包根目录完整路径，不能按文件名后缀取第一个匹配项。
